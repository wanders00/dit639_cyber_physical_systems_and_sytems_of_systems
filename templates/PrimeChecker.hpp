// This is a header file for Prime Checker
#ifndef PRIMECHECKER
#define PRIMECHECKER
#include <cstdint>

// Defining the Prime Checker class
class PrimeChecker {
   public:

    // Defining the isPrime function that takes a 16 bit number
    bool isPrime(uint16_t n);
};
#endif
